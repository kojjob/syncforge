#!/bin/sh

# Configure BEAM clustering for Fly.io
# Uses DNS-based node discovery within the Fly.io private network
if [ -n "$FLY_APP_NAME" ]; then
  export RELEASE_DISTRIBUTION=name
  export RELEASE_NODE="${FLY_APP_NAME}@${FLY_PRIVATE_IP}"
fi
